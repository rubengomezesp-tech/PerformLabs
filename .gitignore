.next
node_modules
dist
.env
.env.local
.env.*.local
.env.development
.env.production
.env.test
!.env.example
*.pem
*.key
supabase/.temp
.vercel
.agents
.claude/skills
skills-lock.json
.DS_Store
*.log
*.tsbuildinfo

# local agent worktrees & scratch
.claude/worktrees/
handoff.md

# obsidian vault mounted on the repo (local notes, not product code)
.obsidian/

# unrelated nested experiment (@miniverse Vite app) — not part of the product;
# excluded from tsconfig too. Keep out of the repo to avoid accidental commits.
performlabs/
